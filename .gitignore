node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Firebase service account (ความลับ ห้าม commit)
serviceAccountKey.json
*serviceAccount*.json

# ข้อมูล sandbox local (รัน dev) — ไม่ขึ้น production
db.json

# รูปแนบที่ผู้ใช้อัปโหลด (หลักฐาน/ความลับลูกค้า) — เก็บบน NAS เท่านั้น ห้ามขึ้น repo
uploads/
jastran-data/

# external agent skills (ไม่เกี่ยวกับโปรเจกต์)
.agent/

# transcript ของ Codex (มี session id / path ในเครื่อง / บทสนทนาภายใน) — ห้ามขึ้น repo
.codex-history/

# ข้อมูล/เอกสาร ไม่ต้องขึ้น repo (ไฟล์ใหญ่/ชั่วคราว)
Excel/
*.xls
*.xlsx
*.docx
~$*
.cache-snapshot.json
dist-*.zip
# zip สำหรับ deploy ขึ้น NAS — สร้างใหม่ได้เสมอด้วย npm run build ไม่ต้องเก็บใน repo
deploy-*.zip

# Agent Skills (ติดตั้งจาก npx skills — ไม่ใช่โค้ดของโปรเจ็ค)
.agents/
.claude/skills/
skills-lock.json

# ไฟล์ token ของ agent จัสทราน (ความลับ ห้ามขึ้น git)
agent-token.txt
