// ============================================================================
// พื้นที่ให้บริการของสาขา (serviceAreaText) — parse + ตรวจ ปลายทางอยู่ในพื้นที่ไหม
// ใช้ร่วม frontend (ตรวจตอนบันทึกใบ) + backend (กรองปลายทางผิดในรายงานต้นทุนน้ำมัน)
// รูปแบบ text: บรรทัดละ "จังหวัด" (ทั้งจังหวัด) หรือ "จังหวัด: อำเภอ1, อำเภอ2" (เฉพาะอำเภอ)
// ============================================================================
const _normP = (s: string) => (s || '').replace(/\s/g, '');

// ตัดคำนำหน้า "จ./จังหวัด" ออกก่อนเทียบ — ใช้เฉพาะตอนเทียบ "ตัวย่อจังหวัด" แบบเป๊ะ
// (เทียบเป๊ะจึงต้องตัดคำนำหน้าเอง ต่างจากการเทียบชื่อเต็มที่ใช้ substring อยู่แล้ว)
// เคสจริงยังไม่พบ (0/7,830 จุด) แต่คนแก้ในหน้าตรวจสอบพิมพ์ "จ.นว" ได้ -> กันไว้ก่อน
const _stripProvPrefix = (s: string) => _normP(s).replace(/^(จังหวัด|จ\.)/, '');

export function parseServiceAreas(text?: string): { prov: string; dists: string[] | null }[] {
  return (text || '').split('\n').map((l) => l.trim()).filter(Boolean).map((line) => {
    const idx = line.search(/[:：]/);
    if (idx < 0) return { prov: line.trim(), dists: null };
    const dists = line.slice(idx + 1).split(/[,，]/).map((s) => s.trim()).filter(Boolean);
    return { prov: line.slice(0, idx).trim(), dists: dists.length ? dists : null };
  });
}

export function inServiceArea(areas: { prov: string; dists: string[] | null }[], prov: string, dist: string): boolean {
  if (!areas.length) return true; // ไม่ตั้งพื้นที่ = ไม่ตรวจ
  const np = _normP(prov), nd = _normP(dist);
  for (const a of areas) {
    const ap = _normP(a.prov);
    if (np && ap && (np.includes(ap) || ap.includes(np))) {
      if (!a.dists) return true; // ทั้งจังหวัด
      return a.dists.some((d) => { const ad = _normP(d); return nd && ad && (nd.includes(ad) || ad.includes(nd)); });
    }
  }
  return false;
}

// ============================================================================
// 🔁 จับ "บิลซ้ำจากจัสทราน" — จุดที่สาขาไม่มีทางวิ่งได้จริง
// ----------------------------------------------------------------------------
// ที่มา (เคสจริง ส.ค.69): บิลสหพัฒน์เลขขึ้นต้น E ต้องคีย์พร้อมปี เช่น "E048130/69"
// ถ้าคนคีย์ลืมใส่ "/69" จัสทรานจะไปจับ "บิลเก่าปีก่อน" ที่เลข E ตรงกันมาผูกให้แทน
// -> จุดส่งของสาขาอื่น (คนละภาค) หลุดเข้ามาในใบกระจาย = คิดเงินผิด
// ตัวอย่าง: E048130 ไม่มีทับ -> ดึง B0425007967 (กำแพงเพชร ปี 68) เข้าใบสาย3
//
// เกณฑ์ตัดสิน: "สาขานี้ไม่มีราคาจังหวัดนี้เลยใน Master ราคาขนส่ง"
//   = สาขาไม่เคยวิ่งจังหวัดนี้ -> เป็นบิลซ้ำแน่นอน ไม่ใช่งานจริง
//
// ⚠️ ทำไมไม่ใช้เกณฑ์อื่น (ตรวจกับข้อมูลจริง 1,012 ใบ / 7,830 จุด แล้ว):
//   - "รหัสสาขาในเลขใบรับต่างจากใบกระจาย" -> โดน 68.7% ของใบ (เลขใบรับคือสาขา
//     "ต้นทางที่ออกบิล" ไม่ใช่ปลายทาง บิลออกที่สาย3 แล้วพิษณุโลกวิ่งส่งเป็นเรื่องปกติ)
//   - "จังหวัดส่วนน้อยในใบ" -> โดน 2.9% ซึ่งส่วนใหญ่เป็นจังหวัดข้างเคียงที่วิ่งจริง
//     (เชียงใหม่->ลำพูน, นครสวรรค์->ชัยนาท) ถ้าบล็อกจะจ่ายเงินขาด
//   เกณฑ์ที่ใช้จริงนี้โดนแค่ 0.40% ของใบ (4/1012) — แม่นพอจะบล็อกได้
//
// ⚠️ ทำไมไม่กรองด้วย "ยอดเงินจุดนั้น" (เคยลองแล้วใช้ไม่ได้):
//   จุดกำแพงเพชรในใบสาย3 (เคสจริงที่ต้องจับ) ได้ receiptAmount = 0 เท่ากับ
//   จุดกรุงเทพฯ คืนของต้นทาง (เคสที่ไม่ควรจับ) เพราะทั้งคู่หาราคาไม่เจอเหมือนกัน
//   -> ยอดเงินแยกสองเคสนี้ไม่ได้ ต้องยกเว้นเป็น "รายจังหวัด" แทน
// ============================================================================

// จังหวัดที่ทุกสาขาแวะได้โดยไม่ต้องมีราคา — ไม่นับเป็นบิลซ้ำ
// กรุงเทพฯ = จุด "คืนของต้นทาง" (ข้อมูลจริง: 4 จุดในใบพิษณุโลก/กำแพงเพชร ยอด ฿0 ทั้งหมด)
const DEFAULT_EXEMPT_PROVINCES = ['กรุงเทพมหานคร', 'กรุงเทพ'];
// งาน "เก็บสินค้าคืน" = ขากลับ ไม่ใช่จุดกระจาย -> ปลายทางอยู่นอกพื้นที่ได้เป็นปกติ
//   รถวิ่งไปส่งของแล้วเก็บของกลับมาเข้าคลัง คลังอาจอยู่คนละจังหวัดกับพื้นที่สาขา
//   เคสจริง (ตรวจ 3 ก.ย.69): JB0426035497 สาขากำแพงเพชร มีจุด "สินค้าเก็บคืน"
//     ปลายทาง ลำลูกกา ปทุมธานี (กำแพงเพชรไม่มีราคาปทุมธานี) แต่ "ถูกต้อง" —
//     มีราคา collect_back ตั้งไว้เฉพาะผู้ส่ง "ทู ทวิน" ฿9.60/ชิ้น = 16 ชิ้น ฿153.60
//   ถ้าไม่ยกเว้น -> ไฮไลท์ส้มเตือนผิด + ด่านบันทึกบล็อก = งานที่ควรได้เงินบันทึกไม่ได้
// ⚠️ ห้ามตัดสิน "เป็นเก็บคืนไหม" ที่ไฟล์นี้ — ต้องรับ isCollectBack จากผู้เรียกเท่านั้น
//   เกณฑ์จริงอยู่ที่ calc.ts:makeCollectBackCheck (ต้องผ่าน 3 ชั้น):
//     1) ทุกรายการในใบรับต้องเป็นเก็บคืน (ปนสินค้าปกติ = ไม่ยกเว้น)
//     2) มีราคา collect_back รองรับ (หรือสาขาแบบครึ่งราคา)
//     3) ราคานั้นต้องอยู่ในกลุ่มราคา (rateGroup) ของรถคันนั้น
//   เคยพลาดมาแล้วทั้ง 3 ชั้น — ถ้าลอกเกณฑ์มาไว้ที่นี่ วันหนึ่งจะเพี้ยนจาก calc.ts อีก
//   **ไม่ส่ง isCollectBack มา = ไม่ยกเว้นเลย** (ปลอดภัยไว้ก่อน ดีกว่าปล่อยของที่ควรจับให้หลุด)
export function suspectDupReceipts<T extends { provinceRaw?: string; receiptNo?: string; totalQty?: number }>(
  receipts: T[],
  branchProvinces: Set<string> | string[],
  branchProvinceShorts: Set<string> | string[] = [],
  exemptProvinces: string[] = DEFAULT_EXEMPT_PROVINCES,
  isCollectBack?: (r: T) => boolean,
): T[] {
  const known = branchProvinces instanceof Set ? branchProvinces : new Set(branchProvinces);
  // สาขายังไม่ได้ตั้งราคาเลย -> ไม่มีข้อมูลพอจะตัดสิน ไม่เตือน (กันเตือนมั่วตอนตั้งสาขาใหม่)
  if (!known.size) return [];
  const normed = new Set(Array.from(known).map((p) => _normP(p)));
  const exempt = exemptProvinces.map((p) => _normP(p)).filter(Boolean);
  // ⚠️ ตัวย่อจังหวัดต้องเทียบ "เป๊ะเท่านั้น" ห้าม substring
  //    เพราะตัวย่อสั้นมากจนไปโผล่ในชื่อจังหวัดอื่น (ตรวจข้อมูลจริงแล้ว):
  //      "พช" (เพชรบูรณ์) อยู่ใน กำแพงเพชร, เพชรบุรี
  //      "ชร" (เชียงราย) อยู่ใน เพชรบูรณ์, กำแพงเพชร, เพชรบุรี
  //      "ลพ" (ลำพูน) อยู่ใน ลพบุรี   ·   "พร" (แพร่) อยู่ใน สุพรรณบุรี
  //    ถ้าเทียบแบบ substring จุดกำแพงเพชรจะ "ผ่าน" เพราะมี "พช"/"ชร" = บั๊กที่ต้องจับหลุด
  const shorts = new Set(
    (branchProvinceShorts instanceof Set ? Array.from(branchProvinceShorts) : branchProvinceShorts)
      .map((p) => _stripProvPrefix(p)).filter(Boolean)
  );
  return (receipts || []).filter((r) => {
    // แถวที่ไม่มีของ (qty 0) -> ข้าม ให้ตรงกับด่าน offArea ที่กรอง totalQty>0 อยู่แล้ว
    // ตรวจแล้ว 4 บิลจริงที่ต้องจับมีของทุกใบ (qty 8/15/17/5) -> กรองนี้ไม่ทำให้พลาดเคสจริง
    // ถ้าไม่ส่ง totalQty มา (undefined) จะยังตรวจตามปกติ ไม่หลุดเงียบ
    if (r?.totalQty != null && !(Number(r.totalQty) > 0)) return false;
    // งานเก็บคืนที่ "คิดเงินเป็นเก็บคืนได้จริง" = ขากลับ ปลายทางนอกพื้นที่ได้
    // (ต้องผ่านตัวตัดสินของผู้เรียก ไม่ใช่แค่ชื่อสินค้า — ดูเหตุผลด้านบน)
    if (isCollectBack && isCollectBack(r)) return false;
    const p = _normP(r?.provinceRaw || '');
    if (!p) return false;                 // ไม่มีจังหวัด -> ไม่ตัดสิน
    // จังหวัดยกเว้น (กรุงเทพฯ = จุดคืนของต้นทาง ทุกสาขาแวะได้ ไม่ใช่บิลซ้ำ)
    for (const e of exempt) if (p.includes(e) || e.includes(p)) return false;
    // ใบเขียนจังหวัดเป็นตัวย่อ (เช่น "นว" = นครสวรรค์) — ตรงเป๊ะเท่านั้น
    // ตัด "จ./จังหวัด" นำหน้าก่อนเทียบ ("จ.นว" ต้องตรงกับตัวย่อ "นว")
    if (shorts.has(_stripProvPrefix(p))) return false;
    // เทียบแบบ "ตรงกันจริง" ทั้งสองทาง เหมือน inServiceArea
    // ตรวจข้อมูลจริง 40 จังหวัดที่ใช้งานอยู่แล้ว ไม่มีคู่ไหน substring ชนกันเลย
    for (const k of normed) if (p.includes(k) || k.includes(p)) return false;
    return true;
  });
}
